# Historical split definitions

These files are immutable provenance for pre-full-public experiments. They are
not runtime alternatives and no maintained command selects them by default.
The current factor and multitask splits are named by
`../canonical_datasets.json`.

Do not modify validation/test membership in these files. Historical experiment
records may cite them by basename; new experiments must use the canonical role
map or an explicitly preregistered split.
